# Optional: Google Analytics 4 (gtag). Create a Web data stream and copy the Measurement ID (G-XXXXXXXX).
# Vite only exposes variables prefixed with VITE_ to the client bundle.
VITE_GA_MEASUREMENT_ID=
